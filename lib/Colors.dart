import 'package:flutter/material.dart';

// ⚡ Rename this class to AppColors to avoid conflict with Flutter's Colors class
class AppColors {
  static const Color primary = Color(0xFF0D47A1);     // Deep Blue (Professional)
  static const Color secondary = Color(0xFF00897B);   // Teal (Energy, Fitness vibe)
  static const Color accent = Color(0xFFFF6F00);      // Deep Orange (Motivation / Highlight)
  static const Color background = Color(0xFFF9FAFB);  // Very Light Grey (Clean UI)

  static const Color textDark = Color(0xFF1E1E1E);    // Dark Grey (Readable text)
  static const Color textLight = Color(0xFFFFFFFF);   // White

  static const Color success = Color(0xFF43A047);     // Healthy Green
  static const Color error = Color(0xFFE53935);       // Refined Red
  static const Color warning = Color(0xFFFFB300);     // Amber-Orange
  static const Color info = Color(0xFF1E88E5);        // Medium Blue
}
